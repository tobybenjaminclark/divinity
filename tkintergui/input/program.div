type int50(g: i32) where {
    g == 50,
};

type intUnder40(g: i32) where {
    g < 40,
};

fn takesInt50(a: int50) -> i32 {
    return(50);
}

fn main () -> i32 {
    a: i32;
    a := takesInt50(25 * 3);
    return(a)
};